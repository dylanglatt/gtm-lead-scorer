# Development only — the tests and the screenshot script. Deliberately NOT in
# requirements.txt: a host installing the app should not build a browser toolchain.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=8.0
playwright>=1.44
